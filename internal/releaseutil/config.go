package main

const (
	releaseToolName = "camp-buzz"

	// Releases are enabled so marketplace can install from GitHub tags/assets.
	releasesEnabled = true

	releaseDisabledReason = "camp-buzz releases are enabled"
)

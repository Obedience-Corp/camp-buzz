# Example fest hooks snippet for Buzz status projection.
# Enable only when camp-buzz and buzz CLI are installed and doctor is clean.
# Fail-open: keep timeouts short; do not block fest on post failures.
#
# Map definition names to real fest lifecycle events for your fest version
# (see: fest hooks list).

hooks:
  definitions:
    buzz_status:
      command: ["camp", "buzz", "post", "--from-hook"]
      timeout: 30s
